# TheMauler — Feature Backlog

The original 10 UI features (bottom table) are implemented. Active work continues in the
regression log (R-series) and UI Ops tracker (U-series) below — see each row's Status.

---

## Active Regression Log - 2026-05-28

These are the current issues to fix before adding new agent foundation features.

### Run Review Fixes - 2026-06-30

| # | Issue | Evidence | Status |
|---|-------|----------|--------|
| R21 | No-think autonomous runs can narrate the next action and stop without a tool call. | `RUN-REVIEW-2026-06-30.md` found repeated `auto_continue_exhausted` runs where Qwen said it would act but emitted no tool call. | Fixed: no-think defaults now use `presence_penalty=0.0`, repeated narration-only recovery applies a one-shot `tool_choice="required"` latch, and missing backend usage gets a local token estimate instead of zeroed task-run tokens. |
| R22 | Repeated shell failure guard can be too sensitive after mixed success/error runs. | Run review noted a shell-heavy run with many successful calls but a `repeated_tool_failure` stop. | Fixed: same-command `done` results now reset the repeated-failure streak, with regression coverage. |
| R23 | Benchmark context rows can reuse an already-loaded larger context instead of reloading the requested smaller context. | Run review noted 8k/16k benchmark confusion and InferenceBridge's lower/equal-context reuse behavior. | Fixed: TheMauler benchmark loads call a forced reload path when available; InferenceBridge `/v1/models/load` accepts `force_reload` / `forceReload` and bypasses reuse for exact benchmark loads. |
| R24 | AgentEval pass/fail suite results are not persisted for later review. | Run review could not inspect exact UI test outcomes because the harness suppresses events and writes no report. | Fixed: AgentEval reports persist to `agent-eval-runs.json`, and Storage can list/clear that history. |
| R25 | Benchmark profile/provider selection can leave a stale provider selected after switching profiles. | The ctx-probe review showed placeholder/default endpoint data contaminating benchmark history. | Fixed: the Benchmarks tab now re-syncs the selected provider from the selected profile. |
| R26 | AgentEval flat pass/fail hides correct artifacts with messy run hygiene. | Run review noted correct files can fail only because `auto_continues` exceeded the cap. | Fixed: AgentEval now reports separate `status_pass`, `artifact_pass`, and `hygiene_pass` fields, and the Benchmarks UI shows them as per-test chips. |
| R27 | Memory conflict events can be too noisy during long runs. | Run review noted many stale-memory conflict events/details in one Ops run. | Fixed: memory conflict event detail is deduplicated and capped to five examples with an omitted-count summary. |
| R28 | System prompt and full tool schema still take too much of the context window. | Run review measured heavy prompt/tool budget, and `buildSystemPrompt` still repeats per-tool terminal/http/file/web workflow prose every turn. | Fixed first pass: terminal/http/file/web/reverse-shell workflow detail moved into tool descriptions, `buildSystemPrompt` keeps compact routing/loop discipline, regression tests cover the split, and production exe rebuilt. |
| R29 | AI Commands panel can show `0 recent` while shell tool calls/results are visible in chat. | Screenshot showed `shell` tool calls/results in Chat, but the bottom AI Commands panel stayed empty because isolated shell tool events did not emit terminal-command events. | Fixed: TerminalPane now also listens to `mauler:tool_call` / `mauler:tool_result` for command-like tools and shows command text, status, exit code, and result previews even when execution is not through the shared PTY. |
| R30 | Ops mode can still advertise too many tools even when the active access preset is unrestricted. | Run review and screenshots showed 30-40+ tools in local-model prompts, which bloats context and hurts Qwen tool discipline. | Fixed first pass: the tool router now treats unrestricted as permission, not "advertise everything"; Ops first turns get a lean shell/read/http/memory core, web/browser tools require explicit intent, and specialist tools broaden on continuation/recovery. |
| R31 | Shell failures can stop Ops too early and isolated shell output is visually disconnected from the Terminal pane. | Latest HTB run repeated `curl` to the webshell, received exit code 7, then hard-stopped with a final report while Terminal showed no output except AI Commands. | Fixed first pass: repeated shell failures are recoverable skips before hard stop, shared-terminal fallback is marked in shell results, and TerminalPane prints isolated shell result previews for command-like tool results. |
| R32 | AI Commands panel width is fixed and the shared terminal can remain falsely busy after output previews, forcing manual Ctrl+C. | Latest screenshot showed the AI Commands rail taking too much horizontal space and multiple isolated fallbacks because the shared terminal was considered busy even though a prompt was visible. | Fixed and built: AI Commands now has a draggable persisted resize handle, and shared-terminal readiness accepts a recent shell prompt followed by preview/result lines while still rejecting real interactive prompts. |
| R33 | Prompt budget telemetry only warned on percentage, so large-context runs could hide bloated system or tool schemas. | Fix 1.6 called for explicit targets of system <=3k tokens and tools <=1.5k tokens, but the app only flagged system+tools over 20 percent. | Fixed and built: prompt budget snapshots now warn on system >3k, tool schema >1.5k, or system+tools >20%; Brain and Logs show used/target values. |
| R34 | Ops path handling regressed when shared-terminal shell fell back to isolated execution. | Latest screenshot showed the agent looking for `/root/HTB_writeups/scripts/` even though the Agent Root is `C:/Users/richa/Documents/HTB_writeups`; after missing-path errors it drifted into reports/questions instead of discovering from the current workspace. | Fixed and built: WSL prompts and isolated fallback results now include the host and WSL workspace paths, and missing-path shell failures tell the model to use `pwd`, relative paths, and `find .` rather than guessed `/root/<workspace>` paths. |
| R35 | Ops auto-continue broadens back to a kitchen-sink toolset. | Last run started around 17 selected tools, then after one continuation advertised 32 tools and tool schema rose to about 8.6k tokens, hurting Qwen tool discipline. | Fixed and built: Ops continuations stay on the lean shell/read/http/evidence set unless the user explicitly asks for research/browser/docs/code work. |
| R36 | Context recovery/goal reminders are too verbose and re-inject noisy shell transport text. | Last run goal reminders included isolated-fallback banners, curl progress/header noise, completed stale plan lines, and pushed system prompt estimates from ~6.3k to ~9.1k tokens. | Fixed and built: goal reminders now keep only open plan items and skip shared-terminal fallback/DNS/progress noise while preserving useful evidence. |
| R37 | Repeated-shell skip recovery returns too much duplicated evidence. | The duplicate curl guard correctly skipped a repeated command, but returned a long evidence blob and recovery text, adding context bloat after a successful guardrail. | Fixed and built: repeated identical shell-result recovery now returns a short evidence summary. |
| R38 | There is no gentle shared-terminal recovery control, so a falsely busy agent terminal pushes the user toward manual Ctrl+C, Restart, or Kill. | Recent HTB runs repeatedly fell back to isolated shell because the shared terminal looked busy, and the UI only exposed heavy controls. | Fixed and built: added `RecoverSharedTerminal` plus Terminal/AI Commands Recover buttons that send Ctrl+C, probe the prompt, and report ready/busy without deleting the session when it can be preserved. |
| R39 | Terminal readiness is still invisible to the user and unavailable as a first-class state machine. | The app could internally reject shared-terminal use as busy, but the UI only showed raw terminal/AI-command streams, so the user could not tell ready vs running vs listener vs prompt. | Fixed and built: added read-only `GetSharedTerminalState` with `missing/ready/running/listener/interactive_prompt/busy/closed` states, a Terminal pane badge, and regression tests. |
| R40 | Prompt cleanup needs to preserve working behavior while reducing the one giant prompt block. | Recent prompt/tool budget tuning showed `buildSystemPrompt` was easier to regress because shell/Ops guidance lived inline with core rules. | Fixed first pass and built: shell routing and Ops guidance are factored into compact prompt fragment builders, keeping existing tested guidance but making future externalization/summing safer. |
| R41 | Tool Registry v2 metadata exists but callers still lack a stable typed registry read path. | Tool metadata was present, but routers/UI/diagnostics had to work from legacy `Tool` values or LLM tool defs. | Fixed and built: added typed `ToolSpec`, stable sorted `Specs`, gated `SpecsFor`, and tests matching the existing enabled/allowed tool selection rules. |
| R42 | Shared-terminal fallback tells the model too little about why it fell back. | The app could fall back to isolated shell because the shared terminal was busy/listening/prompting, but the model only saw a generic fallback marker and often diagnosed the target/network instead. | Fixed in source: fallback notes now include the terminal state plus routing guidance, e.g. use `terminal_read`/`terminal_send` for listeners or interactive prompts, and tests cover the marker. |
| R43 | Doctor launch-flag guidance is too generic for Qwen/llama.cpp agent stability. | U3 called for concrete checks around Jinja, Qwen reasoning format, flash attention, speculative decoding, and quant reliability. | Fixed first pass in source: Doctor inspects `/props` for launch/config signals, warns on speculative/draft decoding, reports Jinja/reasoning/flash state, parses quant tags, and keeps UD-Q4_K_XL as the RTX 3090 default guidance. |
| R44 | `.mauler/progress.md` exists but fresh prompts do not automatically use it for continuity. | U6 progress artifact work had tool/update hooks, but a cold run still had to rely on memory/session recall unless the model explicitly read progress. | Fixed in source: `buildSystemPrompt` injects a compact workspace progress artifact when `.mauler/progress.md` exists, capped with a read-more hint, with regression coverage. |
| R45 | Tool-result aggregate context cap was still high enough to crowd a 40k local context. | Claude review found recent runs near 28-31k prompt tokens and live settings still had `tool_result_aggregate_chars = 200000`. | Fixed in source and live config: default/migration now use 24k chars, and `C:\Users\richa\.config\mauler\settings.toml` was updated to 24000. |
| R46 | Repeat guards could turn successful cached evidence into blocked runs. | Latest HTB runs showed repeated same-result/read guards causing stops/questions instead of letting the agent continue from the cached evidence. | Fixed in source: repeated successful shell/read/empty results remain recoverable skips, include cached previews, and no longer hard-stop; repeated true shell failures can still hard-stop if the model ignores recovery repeatedly. |
| R47 | Doctor needs to prove actual loaded context, not just profile intent. | Claude flagged old benchmark runs where requests said 32k but backend reported actual `n_ctx=8192`. | Fixed in source: Doctor context check now reads `/slots`, `/props`, InferenceBridge `/v1/models/stats`, and `/v1/health`; severe undersize such as 40k requested/8k loaded is a red `fail`. |
| R48 | Duplicate read/shell calls should return a per-run cache hit before executing. | Repeat guards reduced loops but still acted after repeated execution; a true cache should stop identical calls from hitting terminal/network again. | Fixed first pass in source: exact idempotent read/search calls and exact shell commands now return a cached result from the current run; writes/edits and failed prior results are excluded. |
| R49 | Evidence should survive compaction as compact ledger facts. | Claude recommended pinned evidence for target IP, working webshell URL, creds, files, foothold, and failed attempts instead of noisy chat replay. | Fixed first pass in source: successful tool results emit review-required `evidence_pin` ledger events for compact ops facts such as target IPs, webshell URLs, UID output, CVEs, credential-looking lines, flags, and artifact paths. |
| R50 | Shared terminal state should be visible in health checks. | Runs still got confused by listener/running/ready/busy terminal states and sometimes required manual Ctrl+C. | Fixed first pass in source: Doctor reports shared-terminal state as ok/warn/info with guidance for Recover, terminal_read/terminal_send, and background jobs. |
| R51 | Benchmark proof suite still needs a dedicated context/tool-reliability pass. | Claude asked for 40k vs 65k actual context, TTFT/inter-token latency, tool-call reliability, prompt budget, and Q4_K_S vs stronger quant comparison. | Planned next: add a benchmark preset that force-loads 40k/65k, records actual `n_ctx`, runs mini tool-call reliability cases, and persists TTFT/inter-token/prompt-budget results. |
| R52 | Reliability needs a stricter execution spine, not more scattered features. | Latest review identified handoff failures between model decision, tool selection, terminal/shell execution, result capture, context storage, and the next model turn. | Started: added the Reliability Spine section to `docs/agent-loop-upgrade-roadmap.md` covering deterministic tool/session state, live run facts, verifier loop, phase routing, Ops cockpit cards, and reliability benchmarks. First code slice adds terminal-run state-machine advice so the agent does not type over running/listener/prompting terminals. |
| R53 | `terminal_run` can clobber an active listener or prompt. | User screenshots showed terminal busy/listener confusion and manual Ctrl+C recovery. A freeform `terminal_run` while a listener owns the PTY can break the exact session the agent should preserve. | Fixed first pass in source: `terminal_run` now checks the shared terminal state and returns a compact `tool_state_machine` result with the recommended next tool (`terminal_read`, `terminal_send`, or separate `shell`) instead of typing into `running`, `listener`, `interactive_prompt`, or unclear `busy` states. |
| R54 | Pinned evidence is not yet a live facts packet. | Evidence pins existed as ledger candidates, but the next model turn still had to infer current target/webshell/user/artifacts from chat, memory, or raw logs. | Fixed first pass in source: recent `evidence_pin` ledger events and artifact/file events are condensed into a capped "Current run facts" system-prompt packet with categories for target, webshell, shell-user, credentials, artifacts, files, and failed attempts. |
| R55 | Ops routing is still too broad within different attack phases. | Recon, webshell work, reverse-shell handling, and report/writeup tasks have different required tools, but the router mostly treated them as one Ops bucket. | Fixed first pass in source: Ops prompts now classify into `recon`, `webshell`, `reverse_shell`, `report`, or default phases and advertise narrower phase-specific toolsets. Regression tests cover the main phases and no-browser/no-web/no-run_script defaults. |
| R56 | Critical actions need cheap verification before the agent reports success/failure. | Recent runs inferred working webshells, route changes, and shell state from partial outputs, which led to premature summaries or wrong network diagnoses. | Fixed first pass in source: execution-tool results now get compact `verifier_required` hints for webshell, shell-user/root, and target-route ambiguity so the next model turn performs a cheap verification before treating the fact as settled. |
| R57 | `start_listener` can overwrite an already busy/listening/connected shared terminal. | U8 state-machine work covered `terminal_run`, but listener startup still wrote directly into Term 1 even if a listener or live shell was already present. | Fixed in source: `start_listener` now uses the same execution state machine; it refuses duplicate listener/running/prompt/connected states and returns the recommended next tool. Shared-terminal state now distinguishes `connected` from `listener`, and the UI badge labels/styles `connected`. |
| R58 | `terminal_send` can be misused as untracked command execution. | The model could type a new command into an idle/running terminal instead of using `terminal_run`, making commands/results hard to track and risking terminal fights. | Fixed in source: `terminal_send` now uses the state machine. It routes idle new commands to `terminal_run`, allows prompt/live-session input, allows Ctrl-C/Ctrl-D recovery, and blocks arbitrary sends into running/busy terminals. |
| R59 | Shell one-shots are not state-aware around connected sessions/listeners. | With a connected shell in Term 1, local `shell` calls like `whoami` are usually the wrong execution surface; with a listener active, reverse-shell triggers should be explicitly allowed as separate trigger paths. | Fixed in source: shell calls now consult shared-terminal state. Connected-session commands route to `terminal_send`; listener reverse-shell triggers are allowed and ledgered as separate trigger paths; local one-shot checks remain allowed. |
| R60 | Long-running shell commands still rely on model-chosen timers. | Scans/fuzzers such as `nmap -A`, `ffuf`, `gobuster`, `hydra`, etc. can block until timeout when the model forgets `background=true`. | Fixed in source: obvious long-running shell commands are rewritten to `background=true` before execution, returning a job id instead of blocking on arbitrary timeouts. |
| R61 | Verifier hints are not enforced. | A hint in a tool result can still be ignored if the next model turn finalizes instead of verifying. | Fixed in source: any `verifier_required:*` tool result now appends a compact system verifier gate requiring the next action to be a minimal verifier tool call before final success/failure claims. |
| R62 | Run Facts are model-visible but not first-class in Ops UI. | The model received compact ledger facts, but the operator still had to infer them from text logs/memory. | Fixed in source: Live Ops now reads recent ledger events and renders evidence-backed facts for target, webshell, access, artifacts/files, and failed attempts alongside lab/memory facts. |

| # | Issue | Evidence | Status |
|---|-------|----------|--------|
| R1 | Tool calls are being written as plain assistant text instead of emitted as actual tool calls. | Chat transcript shows `glob("**/*.go")` after the model says it will explore files; no matching tool call appears. Recent task logs also show several zero-tool runs for prompts that should inspect files. Direct backend probe returned assistant `content` containing `<tool_call>{"pattern":"**/*.go"}` even with `tool_choice:"required"`. | Fixed: repaired function-style tool text, Qwen `<tool_call><function=...><parameter=...>` blocks, and malformed single-JSON `<tool_call>{...}` blocks into structured tool calls. |
| R2 | Access preset/toolset state is confusing and may make tools look unavailable. | `settings.toml` had `agents.offline_only = true`, `tools.active_toolset = "offline"`, and browser tools disabled while the Agent UI screenshot highlighted Offline/Balanced area. | Fixed locally: switched saved config to Balanced and re-enabled read-only browser tools. Doctor now reports the active access preset/toolset. |
| R3 | Provider/backend reliability needs clearer diagnosis. | Recent task logs include `model_load_error`, `stream_error`, and `chat_error` from the local OpenAI-compatible backend. `/logs` and `/log` returned 404; `/props`, `/slots`, `/health`, and `/v1/health` worked. `/props` reports `chat_format:"Content-only"` and `/v1/models` reports `template_source:"builtin:fallback"`. | Improved: Doctor now warns when llama.cpp reports `chat_format=Content-only` or a fallback template, because native `tool_calls` may be emitted as text. |
| R4 | Stale todo state leaks between unrelated work. | `~/.config/mauler/todos.json` still contained an old Imagescrub checklist while the current workspace is TheMauler. | Fixed locally: cleared the stale active checklist. Durable workspace-scoped todo storage remains a future polish item. |
| R5 | Doctor exists but is too hidden for this kind of failure. | Backend `RunDoctor` and Agent panel overlay exist, but health findings are not surfaced as a first-class readiness/debug flow. | Partially improved: added Doctor checks for tool-call format and access preset. A full Health view remains planned. |
| R6 | Native tool-call compatibility is brittle across llama.cpp versions. | Recent llama.cpp issues mention `function.arguments` sometimes being returned as an object instead of an OpenAI-compatible JSON string. Mauler also sent assistant tool-call history back with `arguments` as raw JSON. | Fixed: SSE parsing now accepts string or object `arguments`, and outbound assistant tool-call history serializes `arguments` as an OpenAI-compatible string. |
| R7 | InferenceBridge was launching Qwen without llama.cpp Jinja/template mode. | Live process command line lacked `--jinja`, `/props` reported `chat_format:"Content-only"`, and `/v1/models` reported `template_source:"builtin:fallback"` for Qwen3.6-27B. This matches the backend returning textual `<tool_call>{"pattern":"**/*.go"}` instead of structured tool calls; after a Jinja reload, a probe returned fenced `{ "tool": "glob", "input": ... }` JSON as assistant text. | Fixed in `C:\Users\richa\Documents\InferenceBridge`: Qwen managed loads now force `--jinja`, the active AppData config has `use_jinja = true`, Qwen tool prompts specify the exact XML format, and the parser repairs bare JSON `<tool_call>` plus fenced tool/input JSON output. TheMauler mirrors the fenced JSON repair. Live model was reloaded with `use_jinja=true`, but the source-level InferenceBridge parser fixes require rebuilding/restarting the app binary to affect API responses. |
| R8 | Blank TheMauler run after asking "whats in this repo?". | Latest task run `task-2026-05-28T19-40-56+01-00` had `status:"done"` but no `response`, no `summary`, and no tools. A streamed tool probe showed InferenceBridge emitted only whitespace plus hidden Qwen tool markup; the streaming normalizer then failed to extract it because Qwen stopped after `</parameter>` without closing `</function>`. The provider also streams reasoning as `delta.reasoning`, not `delta.reasoning_content`. | Fixed: TheMauler now treats empty model output as recoverable/blocked instead of success, listens for `delta.reasoning`, and preserves assistant tool-call history even when content is empty. InferenceBridge now accepts Qwen function blocks cut at end-of-text, emits streamed `tool_calls`, and passes `--flash-attn on` for current llama-server. Rebuilt and relaunched both production binaries; streamed probe now returns a real `glob` tool call. |
| R9 | InferenceBridge leaked prior OpenAI tool-call history as literal `[tool_calls]` prompt text. | Latest run `task-2026-05-28T20-27-09+01-00` successfully called `read_file`, then the next assistant message displayed `[tool_calls] [{"function":...}]` as normal chat text and no second tool event. The bridge normalized assistant `tool_calls` into a plain `[tool_calls]` marker inside the rendered prompt, so Qwen copied the marker instead of emitting a new tool call. The first parsed Qwen path also contained a leading newline (`"\nmain.go"`). | Fixed in InferenceBridge: assistant tool-call history now renders as profile-correct Qwen/Hermes tool markup, tool results render as Qwen `<tool_response>` history for Qwen profiles, Qwen XML parameters trim template newline padding, and `/v1/debug/logs?limit=N` now exposes the headless log buffer. Rebuilt release, restarted bridge, reloaded Qwen with Jinja, and verified non-streaming plus SSE probes both return real `read_file` tool calls with clean `internal/app/app.go` arguments. |
| R10 | Workspace path was applied to file tools but stale chat/system context still made Qwen ask for files from the previous project. | Explorer showed the image-scrub project with `app.py`, `idea.md`, and `requirements.txt`, but the model called `read_many` on `main.go`, `wails.json`, `internal/app/app.go`, and `frontend/src/App.tsx`; the file tools correctly returned not-found from the selected project. | Fixed: workspace changes through Explorer or Settings now apply cwd, persist the path, and clear active chat/tool context plus rollback state. New runs stamp the current workspace root/top-level files into the system prompt, and missing-file tool results include the current workspace plus a glob/read hint. Regression tests cover workspace switching, Settings `workspace_dir`, prompt stamping, and missing-path hints. |
| R11 | Local-provider compatibility should track current LM Studio/llama.cpp tool and usage APIs. | Current LM Studio and llama.cpp APIs expose streaming usage, parallel tool-call controls, llama.cpp `parse_tool_calls`, LM Studio model capabilities/reasoning metadata, and experimental llama.cpp server-side built-in tools. TheMauler already repairs many local-model tool formats but should request native behavior explicitly where safe. | In progress: OpenAI-compatible requests now send `stream_options.include_usage=true`, `parallel_tool_calls=false` when tools are present, and `parse_tool_calls=true` for llama.cpp tool requests. Doctor now reads LM Studio native model metadata for tool/function capability, reasoning metadata, and loaded context length. Doctor also warns if dangerous llama.cpp server-side file/shell built-ins appear in `/props`. Default providers include SGLang/vLLM OpenAI-compatible presets, launch notes are documented in `MAULER.md`, Qwen thinking defaults now use `presence_penalty=0.0`, Hermes JSON tool-call repair is regression-tested, mutation verification is in place, tool-result guardrails have a first pass, and bounded subagent tools are implemented. Remaining: LSP diagnostics, worktree isolation, and continued Doctor/UI polish. |
| R12 | Mutating tool calls need post-write verification before the agent continues. | P3 review found the app only appended syntax lint output after writes, and rollback snapshots were only taken on the confirmation path, not every autonomous write/edit. That left autonomous mutations with weaker rollback/verification guarantees. | Fixed: write/edit tools now snapshot before execution whenever known mutating tools run, then verify successful mutations on disk. `write_file` checks exact content or append suffix; `edit_file` checks `new_string` landed and warns if `old_string` remains. Existing Go/Python/shell lint still runs after verification. Regression tests cover write, append, edit, and mismatch detection. |
| R13 | Shell failures were hiding the useful stderr/stdout and PowerShell `curl` alias mistakes looked like generic exits. | Chat showed repeated shell results as only `error: exit code 1` for `curl -s ...` and `Invoke-WebRequest ...`, so the model/user could not see whether the failure was syntax, network, aliasing, or stderr output. On Windows PowerShell, `curl` may resolve to `Invoke-WebRequest`, where curl flags such as `-s` are invalid. | Fixed: failed tools now preserve captured stdout/stderr and append the exit error instead of replacing the output. PowerShell shell failures now add a hint when `curl` alias usage is likely, telling the model to use `curl.exe` or proper `Invoke-WebRequest -Uri ... -UseBasicParsing`. Regression tests cover output preservation and curl-alias hints. |
| R14 | Promptware or secret-bearing tool output can be fed straight back into the model. | Fetched docs, browser extracts, repo files, and shell output may include hostile text such as "ignore previous instructions" or credential-looking assignments. Hermes-style agents need a clear tool-output trust boundary before the next model turn. | Fixed first pass: every successful or failed tool result now passes through guardrails before being appended to model history or logged to chat. Suspicious prompt-injection/exfiltration language is labelled as untrusted data, and obvious credential assignments/private-key blocks are redacted. Regression tests cover prompt-injection warnings, assignment redaction, private-key redaction, and benign output passthrough. |
| R15 | UI still hides important agent health/state behind secondary surfaces. | The Agent panel has logs and Doctor controls, but live run phase, diagnostics, collapsed panes, and guardrail notices are not prominent enough during failure debugging. | Fixed first pass: added live `mauler:run_state` in the bottom status bar, a top-bar Doctor action, collapsed Explorer/Agent rails, and clearer chat/log guardrail styling. |
| R17 | Plain greetings can still trigger arbitrary tool calls from local Qwen. | A simple "hello" turn called skill tools and only then answered. `tool_choice:"none"` was not enough because local stacks still saw the tool schema and drifted toward available skills. | Fixed: conversational first turns now send no tool definitions at all. The detection covers general small talk and Q&A while still allowing short task prompts such as "fix bug", "run tests", "read README.md", and "list files" to use tools. |
| R16 | Subagent delegation is only a plan, not a bounded runtime capability. | Tracker called for focused Researcher, Reviewer, Test/Fix, and Summarizer runners with explicit profile, toolset, timeout, context budget, and output contracts. | Fixed first pass: added `subagent_research`, `subagent_review`, `subagent_testfix`, and `subagent_summarize` tools. Each uses scratch history, bounded turns/tool calls/time, an explicit toolset, current workspace context, guardrailed tool results, and a fixed report contract. |
| R18 | Backend inference failures can be misreported and subagent loads can downshift a shared local backend. | Latest run `task-2026-06-03T19-10-00+01-00` ended after InferenceBridge returned `HTTP 500` for `http://127.0.0.1:20688/completion`, but the task log kept an older `tool_disabled` stop reason from an earlier `web_search` attempt. InferenceBridge logs show the parent loaded Qwen at 32768 ctx, then a subagent/request reloaded the same model at 24576 ctx with thinking disabled. | Fixed first pass: terminal model/client errors override older recoverable stop reasons; InferenceBridge records post-failure health/slot/props/stderr diagnostics; same-model lower/equal context requests reuse the larger loaded backend instead of reloading down; Mauler logs runtime mismatches before generation; subagents no longer shrink backend launch ctx; pre-output inference failures get one bounded retry; Doctor warns about shared-backend subagent risk. |
| R19 | Master workflow skills can consume too much context when registered. | Registering a large external master skill caused the bottom token bar to jump because the source could be appended into chat/system context instead of being retrieved only when needed. | Fixed: master/project workflow sources are registered as lazy external skills. The system prompt now carries source metadata only, `skill_view master` returns an outline by default, and focused queries return capped matching excerpts. Regression tests cover no auto-injection and focused external-source retrieval. |
| R20 | The live agent flow needs a first-class ops surface. | Reference UI review on 2026-06-11 showed useful patterns from an agent workbench: live KPIs, operational trace, memory/facts sidebar, operator console, and lab/workspace cards. TheMauler already captures run state, activity, task logs, memory, terminal output, todos, and lab context, but these are split across Chat, Logs, Memory, Agent, and Terminal. | First pass landed: center `Ops` view shows current phase, objective, KPIs, recent tool activity, latest run timeline, workspace/lab context, and next-step/fact style side panels. Follow-ups: live facts extracted during a run, richer terminal/command cards, browser screenshot previews, run replay/debug view, and richer lab run cards. |

## UI Ops Update Tracker - 2026-06-11

| # | Item | Status |
|---|------|--------|
| U1 | Live Ops Center tab for watching the agent in flow | First pass done |
| U2 | Agent operational trace with outcome badges | First pass done |
| U3 | Live memory/facts/open-questions panel | First pass done |
| U4 | Run KPI strip: elapsed, tools, edits, tests, tokens, recoveries | Planned |
| U5 | Workspace/lab run cards with target, shell, agent root, latest artifact | Planned |
| U6 | Operator console cards: AI commands, pinned artifacts, pause/take-over | Planned |
| U7 | Optional compact Ops visual density mode | Planned |
| U8 | Shell timer cancel button and cleaner shared-terminal command display | First pass done |
| U9 | Expand web/exploit research budgets and guidance | First pass done |
| U10 | Harden shared-terminal scan execution with pipefail and no-head scan guidance | First pass done |
| U11 | Fix shared-terminal DONE marker parsing and wrapper leakage in tool results | Fixed (2026-06-13): root cause was a re-introduced `stty -echo` toggle in `writeSharedTerminalWrappedCommand`/`sharedTerminalWrapper` — the TTY echoed the wrapper bytes from the same write burst *before* the shell ran `stty -echo`, eating a variable-length prefix of `M=__MA''ULER_` (leaving `MA''ULER_;`, `LER_;`, …) so `markerEchoSig` no longer matched and the wrapper leaked into the visible terminal. Removed the `stty -echo`/`stty echo` toggle (echo stays on per the design note; full intact prefix is echoed and dropped). Added truncation-proof signatures `${M}` + `set -o pipefail 2>/dev/null` (`isWrapperEchoLine`) used by `uiMarkerFilter` + `isSharedTerminalWrapperEcho` as defense-in-depth. Also registered `__MAULER_RECOVER_` in `matchMarker`/`stripMarkerTokens` so the interrupt-recovery sentinel output no longer leaks to the visible terminal (the echoed recovery command stays suppressed via the `stty echo` signature). Regression tests `TestUIMarkerFilterSuppressesTruncatedWrapperEcho`, `TestUIMarkerFilterStripsRecoverySentinel`. |
| U12 | Treat requested README/writeup/docs updates as living milestone artifacts | First pass done |
| U13 | Durable milestone memory and resume flow for HTB/lab/coding runs | First pass: run-end milestone memory landed |
| U14 | Normalize shell tool command escaping before UI/log/shared-terminal execution | Fixed: shell entities decoded before command events, logs, confirmation, and execution |
| U15 | Treat "hack the box" / user-root lab prompts as deep exploit research for web budgets | Fixed: spaced HTB and user/root language now triggers boosted research budget |
| U16 | Prevent doc-required runs from finishing green without write/edit | Fixed: blocked run status plus file-tool-only documentation recovery turn |
| U17 | Prune stale/moved workspace folders from Explorer instead of resurrecting them | Fixed: missing open-folder paths are filtered and config retargeted to moved HTB folder |
| U18 | Keep registered master skill source internal instead of permanently displaying absolute folder paths | Fixed: UI, summaries, and skill_view labels hide full source path while lazy loading still works |
| U19 | Shared-terminal output cut off / dead zone on the right | Fixed (2026-06-13): the agent often runs commands before/while the terminal pane is laid out, so the fit-based `ShellResize` hadn't run and the PTY stayed narrow — width-aware programs hard-wrapped/truncated output (mangling what the model captured) and the visible xterm showed a right-side dead zone. Widened the initial PTY to 200x50 (`OpenShell`), raised the `ShellResize` floor from 20→80 cols / 5→10 rows so a hidden/narrow pane can't starve autonomous output width, and made `TerminalPane` re-fit across two animation frames + a 120ms timeout when the pane becomes visible so xterm reflows the backlog to the real pane width. |
| U20 | Prompt/tool budget still too heavy after tool routing | Fixed: model-facing tool definitions now strip schema prose (`description`/`title`/`examples`/`default`) and cap tool descriptions while keeping full registry metadata and runtime validation intact. Auto-injected progress now keeps only the latest 900-rune excerpt, and memory prompt lines cap large prior-run/session bodies plus tag spam. Regression tests cover compact tool schemas, progress tailing, and large-memory clipping. |
| U21 | Session state and phase routing still inferred from noisy chat/terminal output | First pass done: listeners and terminal snapshots now persist first-class `AgentSession` records and emit RunLedger `session_state` events; model-facing tool routing emits deduped `tool_routing` events with phase, tool choice, and actual tool count; Live Ops facts surface both so stopped runs are easier to diagnose. |
| U22 | Model does not consume current session/facts strongly enough each turn | First pass done: each model request now gets a request-scoped execution state packet with route phase, terminal state, active sessions, and recent ledger facts. This is not appended to chat history, so it stays fresh without compaction bloat. |
| U23 | Tool results are still prose-shaped and easy for local models to misread | First pass done: terminal/state-machine tools now return a compact `contract` block (`state`, `next_tool`, `evidence`, `do_not_repeat`), and verifier hints cover listener/session/file/root/user/webshell/route claims. |
| U24 | No stability score to compare TheMauler runs against stable agents like Hermes | First pass done: loop metrics now score stability and count repeated inputs, repeated skips, verifier pressure, route width, prompt warnings, and tool errors. Logs show a Run Stability card from `loop_metrics`. |

## Durable Run Memory Plan - 2026-06-11

Goal: make TheMauler reliably remember what it did, what it found, and where to resume, especially for long HTB/lab runs and documentation-driven tasks.

| # | Item | Status |
|---|------|--------|
| M1 | Detect meaningful milestones from tool/run events: recon complete, service found, creds found, shell gained, user flag found, privesc lead, root flag, tests passed, doc updated | First pass done: recon, web research, fetched sources, file updates, flag-path sightings, stop reasons, and resume hints |
| M2 | Auto-save concise project memory after milestones, scoped to the active workspace and tagged by target/box/repo | First pass done: task completion now writes concise workspace-scoped memory entries |
| M3 | Treat requested writeups/READMEs/notes as living artifacts and require a doc update after each major verified milestone | First prompt pass done |
| M4 | Add run-end summary memory: done, blocked reason, next action, important paths/artifacts, commands worth reusing | First pass done: status, stop reason, target, milestones, and next action are persisted |
| M5 | Add an Ops/Memory card showing latest remembered facts, open questions, touched files, current target, and resume hints | Partial: Ops side panel first pass done |
| M6 | Add a "Resume Last Run" helper that pulls latest task log, active todos, project memory, and named writeup sections into a compact prompt | Planned |
| M7 | Add tests that milestone memory does not store secrets unless explicitly allowed, and that memories are workspace/target scoped | Partial: redaction and target-tag tests added |

| # | Feature | Status |
|---|---------|--------|
| 1 | Markdown preview in FileViewer | ✅ Done |
| 2 | FileTree right-click context menu | ✅ Done |
| 3 | Inline chat search (Ctrl+F) | ✅ Done |
| 4 | Token budget warning toast | ✅ Done |
| 5 | FileTree file icons by type | ✅ Done |
| 6 | Session auto-save on every assistant message | ✅ Done |
| 7 | Drag file from FileTree into chat input | ✅ Done |
| 8 | Terminal panel (persistent shell below editor) | ⏭ Deferred |
| 9 | Multi-file tabs in center pane | ✅ Done |
| 10 | Agent task queue (queue multiple prompts) | ✅ Done |

---

## Implementation notes

**1 · Markdown preview** — `FileViewer.tsx`: toggle Preview/Edit button when `language === 'markdown'`;
renders via `react-markdown + remark-gfm`. Three-way render: preview / diff / editor.

**2 · Context menu** — `FileTree.tsx` complete rewrite: right-click opens positioned menu with Copy path,
Rename (inline input), New file, New folder, Delete. Go bindings: `RenameFile`, `DeleteFile`, `CreateFile`, `CreateDir`.
CSS in `FileTree.css`: `.ctx-menu`, `.ctx-danger`, `.ctx-divider`, `.file-rename-input`, `.file-tree-create`.

**3 · Chat search** — `ChatPane.tsx`: `Ctrl+F` toggles search bar at top; filters `visibleMessages` by
substring. Count shown as `N / total`. `Escape` dismisses.

**4 · Budget toast** — `Toast.tsx` + `Toast.css` new components. `App.tsx` checks `GetHistoryStats()` on
each `statsVersion` bump; fires warn toast at 75%, danger toast at 90%. Auto-dismisses after 6 s.
Deduped by threshold key so it doesn't re-fire until fraction drops below the trigger.

**5 · File icons** — embedded in FileTree rewrite: `fileIcon()` maps ~20 extensions to emojis
(🔷 ts/tsx, 🟨 js/jsx, 🐹 go, 🐍 py, 📝 md, {} json, 🎨 css, 🌐 html, ⚡ sh/ps1, 🖼️ images, 📁 dir, …).

**6 · Auto-save** — `app.go`: `autoSave()` helper calls `SaveSession("_autosave")` silently.
Called at the end of every `runAgentLoop` defer, so `_autosave` always holds the last agent run.

**7 · Drag to chat** — `FileTree.tsx`: file nodes are `draggable`, `dragStart` sets `text/plain` = path.
`ChatPane.tsx`: textarea `onDrop` reads path; images are encoded via `EncodeFileBase64` and attached;
text files are appended as `@path` to the input.

**8 · Terminal panel** — Deferred: requires new Go bindings (`OpenShell`, `ShellInput`, `ShellClose`),
PTY/pipe management, and xterm.js or equivalent frontend. Scoped for a future session.

**9 · Multi-file tabs** — `App.tsx`: replaced `openFile: OpenFile | null` with `openFiles: OpenFile[]`
+ `activeFileIdx`. `openOrFocusFile()` deduplicates by path. Tabs strip shows each open file with × close
button. `FileViewer` receives `openFiles[activeFileIdx] ?? null` unchanged.

**10 · Task queue** — `App.tsx`: `taskQueue` state. `ChatPane` sends immediately when idle, enqueues when
streaming. Queued messages show `[Queued] …` in chat. On `stream_done`, first queued item is dequeued and
sent. Send button label shows `Queue (+N)` while streaming with pending items.
